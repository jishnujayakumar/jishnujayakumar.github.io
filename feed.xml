<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" xml:lang="en"><generator uri="https://jekyllrb.com/" version="4.4.1">Jekyll</generator><link href="https://jishnujayakumar.github.io/feed.xml" rel="self" type="application/atom+xml"/><link href="https://jishnujayakumar.github.io/" rel="alternate" type="text/html" hreflang="en"/><updated>2026-06-13T20:21:50+00:00</updated><id>https://jishnujayakumar.github.io/feed.xml</id><title type="html">blank</title><subtitle>Jishnu Jaykumar Padalunkal — PhD student at UT Dallas (IRVL) working on embodied robotics — learning from humans and few examples. Graduating 2027; open to research scientist roles in industry. </subtitle><entry><title type="html">Remapping the Copilot key to Ctrl on Ubuntu 22.04</title><link href="https://jishnujayakumar.github.io/blog/2026/remap-copilot-key-to-ctrl/" rel="alternate" type="text/html" title="Remapping the Copilot key to Ctrl on Ubuntu 22.04"/><published>2026-06-11T00:00:00+00:00</published><updated>2026-06-11T00:00:00+00:00</updated><id>https://jishnujayakumar.github.io/blog/2026/remap-copilot-key-to-ctrl</id><content type="html" xml:base="https://jishnujayakumar.github.io/blog/2026/remap-copilot-key-to-ctrl/"><![CDATA[<p><img src="../../../assets/blog/copilot-key-remap/outside-the-lab.jpg" alt="Lenovo Legion laptop on a table by the window, outside the lab" width="100%" height="auto"/></p> <center>Sitting outside the lab, fixing my keyboard key issue. ☀️</center> <p><br/></p> <hr/> <blockquote> <p><strong>A note before we start:</strong> This post was created with the help of an LLM, so please do a fact check before applying anything. I had attempted this remap several times on my own without success — an LLM-assisted debugging session is what finally worked for me, so I documented the recipe here. Also, this trade is personal: right Ctrl is important to <em>my</em> workflow, which is why I remapped it. If the Copilot key is more useful for your use case, please keep it as it is.</p> </blockquote> <h2 id="index">Index</h2> <ul> <li><a href="#index">Index</a></li> <li><a href="#the-problem-a-key-i-never-press">The Problem: A Key I Never Press</a></li> <li><a href="#trivia-why-does-this-key-exist-at-all">Trivia: Why Does This Key Exist At All?</a></li> <li><a href="#the-twist-it-is-not-actually-a-key">The Twist: It Is Not Actually a Key</a></li> <li><a href="#why-the-usual-tools-fail">Why the Usual Tools Fail</a></li> <li><a href="#the-fix-keyd">The Fix: keyd</a></li> <li><a href="#the-gotcha-that-cost-me-ten-minutes">The Gotcha That Cost Me Ten Minutes</a></li> <li><a href="#verifying-it-works">Verifying It Works</a></li> <li><a href="#notes-and-edge-cases">Notes and Edge Cases</a></li> <li><a href="#closing-thoughts">Closing Thoughts</a></li> </ul> <hr/> <h2 id="the-problem-a-key-i-never-press">The Problem: A Key I Never Press</h2> <p>My Lenovo Legion 5 (15IRX10) has no right Ctrl key. In its place sits the <strong>Copilot key</strong> — the one with the swirly logo next to right Alt. I press Ctrl hundreds of times a day and Copilot exactly zero times, so the trade was obvious: turn the Copilot key into Ctrl. 🎯</p> <p><img src="../../../assets/blog/copilot-key-remap/copilot-keyboard.jpg" alt="Lenovo Legion keyboard with the Copilot key next to the right Alt" width="100%" height="auto"/></p> <center>The mystery key next to right <strong>Alt</strong> — the Copilot key. Prime real estate going to waste.</center> <p><br/></p> <p>How hard could a single key remap be? As it turns out, harder than expected — because of what this key actually is.</p> <hr/> <h2 id="trivia-why-does-this-key-exist-at-all">Trivia: Why Does This Key Exist At All?</h2> <p>The Copilot key is a Microsoft creation, not a laptop-maker’s idea. <a href="https://blogs.windows.com/windowsexperience/2024/01/04/introducing-a-new-copilot-key-to-kick-off-the-year-of-ai-powered-windows-pcs/">Announced on January 4, 2024</a>, Microsoft called it the <em>“first significant change to the Windows PC keyboard in nearly three decades”</em> — the last one being the Windows key itself, introduced in 1994. Pressing it launches Microsoft Copilot, their AI assistant, on Windows 11.</p> <p>It became part of the broader AI-PC wave: <a href="https://winaero.com/intel-the-copilot-key-is-a-mandatory-requirement-for-ai-powered-pcs/">the key was included in Microsoft’s “AI PC” definition</a>, and 2024-onward laptops from Lenovo, Dell, HP, and others adopted it — on compact layouts it typically takes the spot where right Ctrl or the menu key used to live, which is exactly how I met it.</p> <p>The <code class="language-plaintext highlighter-rouge">Win+Shift+F23</code> chord we saw above is a neat piece of engineering history: rather than defining a brand-new keycode (which would have meant touching the USB HID standard and keyboard drivers everywhere), Microsoft <a href="https://learn.microsoft.com/en-us/answers/questions/2286867/how-do-i-force-the-copilot-key-to-become-a-differe">hardwired the key to emit a chord</a> built from keycodes that had sat unused in the HID spec for decades — F23 finally found a job. A clever shortcut for instant compatibility with every OS and driver stack; also the reason remapping it takes chord-matching tricks rather than a one-line keycode swap.</p> <p>Windows 11 later <a href="https://pureinfotech.com/customize-copilot-key-action-windows-11/">added a built-in way to customize the key</a> (Settings → Personalization → Text input) to launch the app of your choice. On Linux the key simply does nothing out of the box — so remapping it is pure upside: you are not giving anything up. 📈</p> <hr/> <h2 id="the-twist-it-is-not-actually-a-key">The Twist: It Is Not Actually a Key</h2> <p>Run keyd’s event monitor and press the Copilot key once:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">sudo </span>keyd <span class="nt">-m</span>
</code></pre></div></div> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>ITE Tech. Inc. ITE Device(8258) Keyboard    048d:c195:da10980f    leftmeta down
ITE Tech. Inc. ITE Device(8258) Keyboard    048d:c195:da10980f    leftshift down
ITE Tech. Inc. ITE Device(8258) Keyboard    048d:c195:da10980f    f23 down
ITE Tech. Inc. ITE Device(8258) Keyboard    048d:c195:da10980f    f23 up
ITE Tech. Inc. ITE Device(8258) Keyboard    048d:c195:da10980f    leftshift up
ITE Tech. Inc. ITE Device(8258) Keyboard    048d:c195:da10980f    leftmeta up
</code></pre></div></div> <p>One physical key, <strong>three</strong> key events. The firmware emits the chord <code class="language-plaintext highlighter-rouge">LeftMeta + LeftShift + F23</code> — the shortcut that launches Copilot on Windows. There is no “Copilot keycode”. The OS never sees a single key; it sees a macro. 🤯</p> <blockquote> <p>💡 The exact chord varies by laptop. Some machines emit only <code class="language-plaintext highlighter-rouge">leftshift+leftmeta</code> without the <code class="language-plaintext highlighter-rouge">f23</code>. Always check with <code class="language-plaintext highlighter-rouge">sudo keyd -m</code> before writing any config.</p> </blockquote> <hr/> <h2 id="why-the-usual-tools-fail">Why the Usual Tools Fail</h2> <p>This firmware-level macro is exactly why the classic remapping approaches fall flat:</p> <ul> <li><strong>xmodmap / GNOME Tweaks</strong>: remap single keysyms. There is no single keysym here — and xmodmap is an X11 utility, so it does nothing for Wayland sessions.</li> <li><strong>udev hwdb</strong>: can remap the <code class="language-plaintext highlighter-rouge">f23</code> scancode to <code class="language-plaintext highlighter-rouge">rightctrl</code>, but the firmware still sends Meta and Shift alongside it. You end up pressing <code class="language-plaintext highlighter-rouge">Meta+Shift+Ctrl</code> — useless for shortcuts.</li> </ul> <p>What we need is something that can match the <em>chord</em> and replace the whole thing. That is precisely what <a href="https://github.com/rvaiya/keyd">keyd</a> does: a kernel-level remapping daemon that grabs input devices, rewrites events, and re-emits them through a virtual keyboard. It works in Wayland, X11, and even the virtual console.</p> <hr/> <h2 id="the-fix-keyd">The Fix: keyd</h2> <p>Ubuntu 22.04 does not ship keyd (<a href="https://launchpad.net/ubuntu/+source/keyd">it only entered the Ubuntu archive in 25.10</a>), but building from source takes under a minute and needs nothing beyond a C compiler and the kernel headers already present on most systems:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">sudo </span>apt <span class="nb">install </span>git build-essential
git clone https://github.com/rvaiya/keyd
<span class="nb">cd </span>keyd
make <span class="o">&amp;&amp;</span> <span class="nb">sudo </span>make <span class="nb">install
sudo </span>systemctl <span class="nb">enable</span> <span class="nt">--now</span> keyd
</code></pre></div></div> <p>Then create <code class="language-plaintext highlighter-rouge">/etc/keyd/default.conf</code>:</p> <div class="language-ini highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nn">[ids]</span><span class="w">
</span><span class="na">*</span><span class="w">

</span><span class="nn">[main]</span><span class="w">
</span><span class="na">leftshift+leftmeta+</span><span class="py">f23</span><span class="w"> </span><span class="p">=</span><span class="w"> </span><span class="s">layer(control)</span>
</code></pre></div></div> <p>The left-hand side is a keyd <strong>chord</strong>: it fires only when all three keys arrive together (within keyd’s chord window), which is exactly how the firmware sends them. The right-hand side activates the control layer, so holding the Copilot key now behaves like holding Ctrl.</p> <p>Apply it:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">sudo </span>keyd reload
</code></pre></div></div> <p>Done. Copilot+C copies, Copilot+T opens a new browser tab, Copilot+L focuses the address bar. The dead key lives. ⚡</p> <hr/> <h2 id="the-gotcha-that-cost-me-ten-minutes">The Gotcha That Cost Me Ten Minutes</h2> <p>After installing keyd I ran <code class="language-plaintext highlighter-rouge">sudo keyd reload</code> and… nothing changed. <code class="language-plaintext highlighter-rouge">keyd -m</code> kept showing the raw <code class="language-plaintext highlighter-rouge">leftmeta/leftshift/f23</code> events with no remapping in sight.</p> <p>The reason was hiding in the journal:</p> <div class="language-bash highlighter-rouge"><div class="highlight"><pre class="highlight"><code>journalctl <span class="nt">-u</span> keyd <span class="nt">--no-pager</span> | <span class="nb">tail</span>
</code></pre></div></div> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>keyd[19693]: DEVICE: ignoring 048d:c195:da10980f  (ITE Tech. Inc. ITE Device(8258) Keyboard)
</code></pre></div></div> <p><code class="language-plaintext highlighter-rouge">make install</code> installs the daemon and the systemd unit, <strong>but it does not create a config file</strong>. With no <code class="language-plaintext highlighter-rouge">/etc/keyd/default.conf</code>, keyd has no <code class="language-plaintext highlighter-rouge">[ids]</code> section to match against, so it politely ignores every keyboard on the system. The service runs, reload succeeds, and nothing happens — a perfectly silent failure. 🤦</p> <p>Two diagnostics worth remembering:</p> <ol> <li><strong><code class="language-plaintext highlighter-rouge">keyd -m</code> shows both sides of the pipeline.</strong> Raw hardware events appear under your physical keyboard’s name; remapped output appears under <code class="language-plaintext highlighter-rouge">keyd virtual keyboard</code>. If you only see the former, keyd is not grabbing your device.</li> <li><strong>The journal names every device decision.</strong> <code class="language-plaintext highlighter-rouge">DEVICE: ignoring ...</code> vs. <code class="language-plaintext highlighter-rouge">DEVICE: match ...</code> tells you instantly whether your config applies to your keyboard.</li> </ol> <hr/> <h2 id="verifying-it-works">Verifying It Works</h2> <p>After writing the config and reloading, the journal flipped from <code class="language-plaintext highlighter-rouge">ignoring</code> to <code class="language-plaintext highlighter-rouge">match</code>:</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>keyd[19693]: DEVICE: match    048d:c195:da10980f  /etc/keyd/default.conf  (ITE Tech. Inc. ITE Device(8258) Keyboard)
keyd[19693]: DEVICE: match    0001:0001:70533846  /etc/keyd/default.conf  (AT Translated Set 2 keyboard)
</code></pre></div></div> <p>And holding Copilot+C over selected text copied it. ✅</p> <hr/> <h2 id="notes-and-edge-cases">Notes and Edge Cases</h2> <ul> <li><strong>It maps to <em>left</em> Ctrl behavior.</strong> keyd’s control layer emits left Ctrl, not right Ctrl. For virtually everything — shortcuts, terminals, IDEs — the two are interchangeable. If some software of yours genuinely distinguishes right Ctrl, look at <a href="https://github.com/m-bartlett/remap-copilot">remap-copilot</a>, a small libevdev daemon built for exactly that.</li> <li><strong>Keep the <code class="language-plaintext highlighter-rouge">f23</code> in the chord.</strong> A two-key <code class="language-plaintext highlighter-rouge">leftshift+leftmeta</code> chord also works on machines that emit no <code class="language-plaintext highlighter-rouge">f23</code>, but on machines that do, the three-key form avoids accidentally hijacking real Shift+Super shortcuts (e.g., GNOME’s Shift+Super+arrow for moving windows between monitors) when you press them near-simultaneously.</li> <li><strong>If holding does not register on your machine</strong>, your firmware may fire the full press+release sequence on key-down. The workaround is <code class="language-plaintext highlighter-rouge">oneshot(control)</code> instead of <code class="language-plaintext highlighter-rouge">layer(control)</code>: tap Copilot, then tap C, and Ctrl+C is emitted.</li> </ul> <hr/> <h2 id="closing-thoughts">Closing Thoughts</h2> <p>The Copilot key turned out to be a delightful little case study in how modern keyboards actually work: what looks like a key can be a macro, and remapping it means reaching for tooling that operates below the display server. keyd handles it in four lines of config — once you know the config file does not create itself. 😄</p> <p>If your keyboard has a key you never press sitting where one you press constantly used to be, now you know how to make it yours.</p> <hr/> <h2 id="references">References</h2> <ul> <li><a href="https://blogs.windows.com/windowsexperience/2024/01/04/introducing-a-new-copilot-key-to-kick-off-the-year-of-ai-powered-windows-pcs/">Microsoft: Introducing a new Copilot key (Windows Experience Blog, Jan 4, 2024)</a></li> <li><a href="https://winaero.com/intel-the-copilot-key-is-a-mandatory-requirement-for-ai-powered-pcs/">Intel: the Copilot key is a requirement under the “AI PC” program definition</a></li> <li><a href="https://pureinfotech.com/customize-copilot-key-action-windows-11/">Pureinfotech: How to customize the Copilot key action on Windows 11</a></li> <li><a href="https://github.com/rvaiya/keyd">keyd — A key remapping daemon for Linux</a></li> <li><a href="https://github.com/m-bartlett/remap-copilot">remap-copilot — libevdev daemon for the Copilot key</a></li> <li><a href="https://forum.manjaro.org/t/how-to-remap-the-copilot-key-to-right-control/172909">Manjaro forum: How to remap the Copilot key to right control</a></li> </ul>]]></content><author><name></name></author><category term="linux"/><category term="phd-life"/><category term="linux"/><category term="keyboard"/><category term="tooling"/><category term="how-to"/><summary type="html"><![CDATA[The Copilot key is not a real key — it is a three-key macro baked into firmware. Here is how to turn it into a proper Ctrl key on Linux using keyd.]]></summary></entry><entry><title type="html">Save raw depth as JPEG or PNG?</title><link href="https://jishnujayakumar.github.io/blog/2024/saving-depth-as-jpg-vs-png/" rel="alternate" type="text/html" title="Save raw depth as JPEG or PNG?"/><published>2024-12-19T00:00:00+00:00</published><updated>2024-12-19T00:00:00+00:00</updated><id>https://jishnujayakumar.github.io/blog/2024/saving-depth-as-jpg-vs-png</id><content type="html" xml:base="https://jishnujayakumar.github.io/blog/2024/saving-depth-as-jpg-vs-png/"><![CDATA[<p><img src="../../../assets/blog/jpg-vs-png/jpg-vs-png-depth-jumbotron.gif" alt="3D point clouds built from JPEG depth vs PNG depth, overlaid in one frame" width="100%" height="auto"/></p> <center>The same scene, twice: point clouds built from <strong style="color:red">JPEG</strong> depth vs <strong style="color:lightgreen">PNG</strong> depth.</center> <p><br/></p> <hr/> <blockquote> <p><strong>TL;DR:</strong> Never save raw depth as JPEG. JPEG gives you 256 depth levels and smears lossy-compression artifacts across object boundaries; 16-bit PNG gives you 65,536 levels, losslessly. Convert to millimeters, cast to <code class="language-plaintext highlighter-rouge">uint16</code>, save as PNG. Your point clouds will thank you.</p> </blockquote> <h2 id="index">Index</h2> <ul> <li><a href="#index">Index</a></li> <li><a href="#the-spark-a-quest-for-compression">The Spark: A Quest for Compression</a></li> <li><a href="#the-twist-shaky-3d-scenes">The Twist: Shaky 3D Scenes</a></li> <li><a href="#rgb--jpeg-depth-scene-point-cloud">RGB + JPEG Depth Scene Point Cloud</a></li> <li><a href="#the-discovery-switching-to-png">The Discovery: Switching to PNG</a></li> <li><a href="#the-evidence-videos-and-point-clouds">The Evidence: Videos and Point Clouds</a></li> <li><a href="#lessons-learned-why-png-wins">Lessons Learned: Why PNG Wins</a></li> <li><a href="#closing-thoughts">Closing Thoughts</a></li> </ul> <hr/> <h2 id="the-spark-a-quest-for-compression">The Spark: A Quest for Compression</h2> <p>In my recent robotics project, data collection was progressing at full steam. The challenge? Managing the ever-growing volume of RGB and depth images. 📈 Compression seemed like the perfect solution. For RGB images, JPEG was a natural choice—efficient, widely used, and capable of reducing file sizes significantly. But then came the question: Could JPEG work for depth images too? 💾</p> <p>Eager to streamline the process, I stored both RGB and depth images in JPEG format. It felt like a practical and straightforward decision at the time.</p> <hr/> <h2 id="the-twist-shaky-3d-scenes">The Twist: Shaky 3D Scenes</h2> <p>Things took an unexpected turn when I moved to the next phase: generating 3D scene point clouds from the depth data. Point clouds are critical for extracting insights and performing spatial analysis. 🧠 Using my JPEG-based dataset, I plotted the point clouds—and that’s when the problems began. The 3D scenes were noisy, unstable, and lacked the precision I needed. ❌</p> <p>Digging deeper, I realized JPEG was hurting the depth data twice. First, bit depth: baseline JPEG stores 8 bits per channel — <a href="https://docs.opencv.org/4.x/d4/da8/group__imgcodecs.html" target="_blank">OpenCV’s <code class="language-plaintext highlighter-rouge">imwrite</code> will only write 8-bit JPEG</a> — so every depth value in the scene gets squeezed into just <strong>256 levels</strong>. Second, JPEG’s compression is <em>lossy</em>: its block-based encoding introduces artifacts exactly where depth matters most — across object boundaries, where a few corrupted pixels turn a clean edge into a spray of floating points. You can see both effects directly in the comparison videos below. Crucial detail was gone before I ever plotted a single point.</p> <p><img src="../../../assets/blog/jpg-vs-png/rgbd-jpeg-3d.gif" width="100%"/></p> <center>RGB + JPEG Depth Scene Point Cloud</center> <hr/> <h2 id="the-discovery-switching-to-png">The Discovery: Switching to PNG</h2> <p>To address the issue, I switched to storing depth images as 16-bit PNG — <a href="https://www.w3.org/TR/png-3/" target="_blank">the PNG specification supports up to 16 bits per channel with lossless compression</a>, and OpenCV writes <code class="language-plaintext highlighter-rouge">uint16</code> PNGs natively. The difference is not subtle: 16 bits means <strong>65,536 distinct depth levels</strong> instead of 256 — a 256× jump in precision — and lossless means every value comes back exactly as it was written. Stored as millimeters in <code class="language-plaintext highlighter-rouge">uint16</code>, that covers 0 to 65.535 meters at 1 mm resolution: more than enough for any indoor robot. With this new setup, I recollected data and generated fresh point clouds. 🎨</p> <p>The results were striking. The PNG-based depth data produced clear, accurate, and stable 3D scenes. The full bit depth preserved the subtle variations in depth values, enabling precise visualization and analysis. 🌄</p> <p>What seemed like a minor choice of file format turned out to have a major impact on the quality and usability of the data.</p> <p><img src="../../../assets/blog/jpg-vs-png/rgbd-png-3d.gif" width="100%"/></p> <center>RGB + PNG Depth Scene Point Cloud</center> <hr/> <h2 id="the-evidence-videos-and-point-clouds">The Evidence: Videos and Point Clouds</h2> <p>To highlight the differences, I created visual comparisons:</p> <video class="demo-video" width="100%" height="auto" controls=""> <source src="../../../assets/blog/jpg-vs-png/output_jpg.mp4" type="video/mp4"/> Your browser does not support the video tag. </video> <center>RGB + JPEG Depth</center> <p><br/></p> <video class="demo-video" width="100%" height="auto" controls=""> <source src="../../../assets/blog/jpg-vs-png/output_png.mp4" type="video/mp4"/> Your browser does not support the video tag. </video> <center>RGB + PNG Depth</center> <p><br/></p> <video class="demo-video" width="100%" height="auto" controls=""> <source src="../../../assets/blog/jpg-vs-png/rgbd-jpg-png.mp4" type="video/mp4"/> Your browser does not support the video tag. </video> <center>3D scene point clouds created using depth data from both JPEG and PNG are shown in a single 3D frame.</center> <p><br/></p> <p>It is evident that JPEG depth loses some depth information, while PNG is better at capturing a wider range of depth values. These visualizations clearly demonstrate why PNG is the superior choice for depth data.</p> <hr/> <h2 id="lessons-learned-why-png-wins">Lessons Learned: Why PNG Wins</h2> <p>Two factors decide this, and JPEG loses on both:</p> <table> <thead> <tr> <th> </th> <th>JPEG</th> <th>16-bit PNG</th> </tr> </thead> <tbody> <tr> <td><strong>Bit depth</strong></td> <td>8-bit → 256 levels</td> <td>16-bit → 65,536 levels</td> </tr> <tr> <td><strong>Compression</strong></td> <td>Lossy — artifacts at depth edges</td> <td>Lossless — bit-exact round trip</td> </tr> <tr> <td><strong>Range @ 1 mm resolution</strong></td> <td>0.255 m 😬</td> <td>65.535 m ✅</td> </tr> <tr> <td><strong>File size</strong></td> <td>Smallest</td> <td>Larger — and worth every byte</td> </tr> </tbody> </table> <p>While JPEG remains excellent for RGB images, raw depth is <em>measurement data</em>, not a photo — it needs every bit back exactly as recorded. The larger file size is real, but it buys you data you can actually trust. 📁</p> <p>Here’s the snippet I use to convert and save depth from the ROS topic on the Fetch robot:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">if</span> <span class="n">depth</span><span class="p">.</span><span class="n">encoding</span> <span class="o">==</span> <span class="sh">"</span><span class="s">32FC1</span><span class="sh">"</span><span class="p">:</span>
    <span class="c1"># Convert depth message to OpenCV format
</span>    <span class="n">depth_cv</span> <span class="o">=</span> <span class="n">self</span><span class="p">.</span><span class="n">cv_bridge</span><span class="p">.</span><span class="nf">imgmsg_to_cv2</span><span class="p">(</span><span class="n">depth</span><span class="p">)</span>

    <span class="c1"># Replace NaN values with 0 and convert from meters to millimeters
</span>    <span class="n">depth_cv</span> <span class="o">=</span> <span class="n">np</span><span class="p">.</span><span class="nf">array</span><span class="p">(</span><span class="n">depth_cv</span><span class="p">)</span>
    <span class="n">depth_cv</span><span class="p">[</span><span class="n">np</span><span class="p">.</span><span class="nf">isnan</span><span class="p">(</span><span class="n">depth_cv</span><span class="p">)]</span> <span class="o">=</span> <span class="mi">0</span>
    <span class="n">depth_cv</span> <span class="o">=</span> <span class="n">depth_cv</span> <span class="o">*</span> <span class="mi">1000</span>

    <span class="c1"># Convert to uint16: 1 mm resolution, 0–65.535 m range
</span>    <span class="n">depth_cv</span> <span class="o">=</span> <span class="n">depth_cv</span><span class="p">.</span><span class="nf">astype</span><span class="p">(</span><span class="n">np</span><span class="p">.</span><span class="n">uint16</span><span class="p">)</span>

    <span class="c1"># Save losslessly as 16-bit PNG
</span>    <span class="n">cv2</span><span class="p">.</span><span class="nf">imwrite</span><span class="p">(</span><span class="sh">"</span><span class="s">depth.png</span><span class="sh">"</span><span class="p">,</span> <span class="n">depth_cv</span><span class="p">)</span>
</code></pre></div></div> <hr/> <h2 id="closing-thoughts">Closing Thoughts</h2> <p>In robotics, even seemingly minor decisions—like choosing a file format—can have far-reaching implications. This one cost me a data recollection; one line of foresight (<code class="language-plaintext highlighter-rouge">cv2.imwrite("depth.png", ...)</code>) would have saved it.</p> <p>Next time you’re saving depth, remember: a depth image is a sensor measurement wearing an image costume. Store it like data—16-bit, lossless—and your point clouds will stand still. ✅</p> <p>If you’ve worked with other formats for depth data, I’d love to hear your insights. 🤝✨</p> <hr/> <h2 id="references">References</h2> <ul> <li><a href="https://docs.opencv.org/4.x/d4/da8/group__imgcodecs.html" target="_blank">OpenCV <code class="language-plaintext highlighter-rouge">imwrite</code> documentation</a> — supported formats and bit depths (8-bit JPEG; 16-bit PNG)</li> <li><a href="https://www.w3.org/TR/png-3/" target="_blank">PNG specification (W3C)</a> — up to 16 bits per channel, lossless</li> <li><a href="https://www.w3.org/Graphics/JPEG/itu-t81.pdf" target="_blank">ITU-T T.81: the JPEG standard</a> — baseline DCT coding is 8-bit, lossy</li> </ul> <hr/> <p>Feel free to reach out in case you have a query. You are always welcome. You can find me on X at <a href="https://x.com/jishnu_jaykumar" target="_blank">@jishnu_jaykumar</a>.</p> <script>document.querySelectorAll(".demo-video").forEach(function(e){e.playbackRate=1.5});</script>]]></content><author><name></name></author><category term="research"/><category term="robotics"/><category term="phd-life"/><category term="case-study"/><category term="depth"/><summary type="html"><![CDATA[A case study of saving raw depth from fetch robot camera as JPEG and PNG]]></summary></entry><entry><title type="html">Setting Up a Wi-Fi Hotspot on Boot for Your Robot</title><link href="https://jishnujayakumar.github.io/blog/2024/setting-up-a-wifi-adapter-on-irvl-fetch/" rel="alternate" type="text/html" title="Setting Up a Wi-Fi Hotspot on Boot for Your Robot"/><published>2024-12-16T00:00:00+00:00</published><updated>2024-12-16T00:00:00+00:00</updated><id>https://jishnujayakumar.github.io/blog/2024/setting-up-a-wifi-adapter-on-irvl-fetch</id><content type="html" xml:base="https://jishnujayakumar.github.io/blog/2024/setting-up-a-wifi-adapter-on-irvl-fetch/"><![CDATA[<p><img src="/assets/blog/irvl-fetch-wifi-adapter-diy/alpha_adapter/robot-human-hololens.png" alt="robot-human-hololens" width="100%"/></p> <center>At the <a href="https://labs.utdallas.edu/irvl/" target="_blank">Intelligent Robotics and Vision Lab</a>, I am trying to establish a connection between the <span style="font-weight: bold">HoloLens 2</span> device mounted on my head and the <span style="font-weight: bold">Fetch</span> robot.</center> <p><br/></p> <hr/> <blockquote> <p><strong>TL;DR:</strong> Three small pieces give your robot a self-hosted Wi-Fi network that survives reboots: a script that brings up an <code class="language-plaintext highlighter-rouge">nmcli</code> hotspot, a <code class="language-plaintext highlighter-rouge">chmod 600</code> credentials file it reads, and a <code class="language-plaintext highlighter-rouge">systemd</code> oneshot service that runs it at boot. Recipe below, tested on our Fetch robot (Ubuntu 18.04).</p> </blockquote> <h2 id="index">Index</h2> <ul> <li><a href="#index">Index</a> <ul> <li><a href="#why-create-a-wi-fi-hotspot-on-boot">Why Create a Wi-Fi Hotspot on Boot?</a></li> <li><a href="#prerequisites">Prerequisites</a></li> <li><a href="#step-1-create-the-hotspot-script">Step 1: Create the Hotspot Script</a></li> <li><a href="#step-2-create-a-credentials-file">Step 2: Create a Credentials File</a></li> <li><a href="#step-3-create-a-systemd-service">Step 3: Create a Systemd Service</a></li> <li><a href="#step-4-verify-hotspot-on-boot">Step 4: Verify Hotspot on Boot</a></li> <li><a href="#step-5-optional--add-status-check-to-bashrc">Step 5: Optional – Add Status Check to ~/.bashrc</a></li> <li><a href="#troubleshooting">Troubleshooting</a></li> </ul> </li> </ul> <hr/> <h3 id="why-create-a-wi-fi-hotspot-on-boot">Why Create a Wi-Fi Hotspot on Boot?</h3> <p>In robotics, self-hosted networks can be critical for:</p> <ul> <li>Connecting remote controllers or operator devices.</li> <li>Transmitting real-time data streams (e.g., images, depth, or control commands).</li> <li>Enabling peer-to-peer communication in isolated environments.</li> </ul> <p>Let’s configure a Wi-Fi hotspot to start automatically whenever your robot boots up. 📡🤖✨</p> <hr/> <h3 id="prerequisites">Prerequisites</h3> <ul> <li>A Linux-based system (tested on Ubuntu). Our Fetch robot has Ubuntu 18.04 installed.</li> <li>NetworkManager installed (pre-installed on Ubuntu desktop editions).</li> <li>A compatible Wi-Fi adapter connected to the robot.</li> <li>Root access to the system.</li> </ul> <p>📋 Ensure these are set up before proceeding!</p> <video class="demo-video" width="100%" height="50%" controls=""> <source src="../../../assets/blog/irvl-fetch-wifi-adapter-diy/alpha_adapter/robot-with-alpha-adapter.mp4" type="video/mp4"/> Your browser does not support the video tag. </video> <center><a href="https://www.amazon.com/dp/B08BJS8FXD" target="_blank">Alfa AWUS036ACM dual-antenna Wi-Fi adapter</a> connected on top of the head of our Fetch robot.</center> <p><br/></p> <hr/> <h3 id="step-1-create-the-hotspot-script">Step 1: Create the Hotspot Script</h3> <p>Let’s have a directory related to this setting:</p> <div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># try setting absolute path</span>
<span class="nb">export </span><span class="nv">WIFI_ADAP_ROOT_DIR</span><span class="o">=</span>/home/fetch/wifi-adapter-settings
<span class="nb">mkdir</span> <span class="nt">-p</span> <span class="nv">$WIFI_ADAP_ROOT_DIR</span>
<span class="nb">cd</span> <span class="nv">$WIFI_ADAP_ROOT_DIR</span>
</code></pre></div></div> <p>We’ll use a Bash script to configure and start the hotspot. Create the file <code class="language-plaintext highlighter-rouge">start_hotspot.sh</code>:</p> <div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c">#!/bin/bash</span>

<span class="c"># Disable the firewall</span>
ufw disable

<span class="nb">echo</span> <span class="s2">"Firewall stopped and disabled on system startup"</span>

<span class="c"># Wait for 2 seconds</span>
<span class="nb">sleep </span>2

<span class="c"># Read credentials and create hotspot</span>
<span class="c"># Use the absolute path here: systemd runs this script with a clean</span>
<span class="c"># environment, so $WIFI_ADAP_ROOT_DIR will NOT be set at boot.</span>
<span class="nv">CREDS_FILE</span><span class="o">=</span>/home/fetch/wifi-adapter-settings/hotspot_creds.cfg
<span class="nb">source</span> <span class="nv">$CREDS_FILE</span>

<span class="c"># Create the hotspot</span>
nmcli device wifi hotspot ifname alpha_wifi_adap ssid <span class="s2">"</span><span class="nv">$SSID</span><span class="s2">"</span> password <span class="s2">"</span><span class="nv">$PASSWORD</span><span class="s2">"</span> band a channel 149
</code></pre></div></div> <p>Explanation of the Script:</p> <ul> <li><strong>Disable the firewall:</strong> Ensures no network traffic is blocked (this is fine on an isolated lab robot — adjust for secure setups).</li> <li><strong>Wait for 2 seconds:</strong> Adds a buffer to ensure system services are up.</li> <li><strong>Source credentials:</strong> Reads the hotspot name (SSID) and password from hotspot_creds.cfg. Use the <strong>absolute path</strong> — when systemd runs this script at boot, shell environment variables like <code class="language-plaintext highlighter-rouge">$WIFI_ADAP_ROOT_DIR</code> are not set.</li> <li><strong>Set up the hotspot:</strong> Uses nmcli to configure and start the hotspot.</li> </ul> <p>Save this script to <code class="language-plaintext highlighter-rouge">$WIFI_ADAP_ROOT_DIR/start_hotspot.sh</code> and make it executable:</p> <div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">chmod</span> +x <span class="nv">$WIFI_ADAP_ROOT_DIR</span>/start_hotspot.sh
</code></pre></div></div> <hr/> <h3 id="step-2-create-a-credentials-file">Step 2: Create a Credentials File</h3> <p>Store your hotspot credentials in a secure configuration file:</p> <div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code>nano <span class="nv">$WIFI_ADAP_ROOT_DIR</span>/hotspot_creds.cfg
</code></pre></div></div> <p>Add the following content:</p> <div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nv">SSID</span><span class="o">=</span><span class="s2">"your_hotspot_name"</span>
<span class="nv">PASSWORD</span><span class="o">=</span><span class="s2">"your_secure_password"</span>
</code></pre></div></div> <p>Make sure the credentials file is readable only by the owner:</p> <div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">chmod </span>600 <span class="nv">$WIFI_ADAP_ROOT_DIR</span>/hotspot_creds.cfg
</code></pre></div></div> <p>🛡️ This keeps your credentials secure!</p> <hr/> <h3 id="step-3-create-a-systemd-service">Step 3: Create a Systemd Service</h3> <p>To ensure the hotspot script runs on every boot, create a systemd service file:</p> <div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">sudo </span>nano /etc/systemd/system/start_hotspot.service
</code></pre></div></div> <p>Add the following content:</p> <div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="o">[</span>Unit]
<span class="nv">Description</span><span class="o">=</span>Start Hotspot Script on Boot
<span class="nv">After</span><span class="o">=</span>network.target

<span class="o">[</span>Service]
<span class="nv">Type</span><span class="o">=</span>oneshot
<span class="nv">ExecStart</span><span class="o">=</span>/bin/bash /home/fetch/wifi-adapter-settings/start_hotspot.sh
<span class="nv">RemainAfterExit</span><span class="o">=</span><span class="nb">yes</span>

<span class="o">[</span>Install]
<span class="nv">WantedBy</span><span class="o">=</span>multi-user.target
</code></pre></div></div> <p>Explanation of the Service File:</p> <ul> <li><strong>After=network.target:</strong> Ensures the service starts after network services are ready.</li> <li><strong>ExecStart:</strong> Runs the hotspot script. <em>NOTE:</em> Keep the absolute path of the script.</li> <li><strong>RemainAfterExit:</strong> Keeps the service active after execution.</li> </ul> <p>Enable the service:</p> <div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">sudo </span>systemctl <span class="nb">enable </span>start_hotspot.service
</code></pre></div></div> <p>Test the service by starting it manually:</p> <div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">sudo </span>systemctl start start_hotspot.service
<span class="nb">sudo </span>systemctl status start_hotspot.service
</code></pre></div></div> <p>If successful, the hotspot should start without errors. 🚀✨</p> <hr/> <h3 id="step-4-verify-hotspot-on-boot">Step 4: Verify Hotspot on Boot</h3> <p>To confirm the hotspot starts correctly after reboot:</p> <ul> <li>Reboot your system: <div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">sudo </span>reboot
</code></pre></div> </div> </li> <li>Once the system boots up, check the hotspot status: <div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">sudo </span>systemctl status start_hotspot.service
</code></pre></div> </div> </li> <li>You can also use the nmcli command to verify: <div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code>nmcli dev wifi
</code></pre></div> </div> </li> </ul> <p>✅ This ensures everything is working smoothly! 🌟</p> <hr/> <h3 id="step-5-optional--add-status-check-to-bashrc">Step 5: Optional – Add Status Check to ~/.bashrc</h3> <p>For convenience, add a status check to your <code class="language-plaintext highlighter-rouge">~/.bashrc</code> file. This will display the hotspot’s status whenever you open a terminal:</p> <div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="c"># Check Hotspot Status, use absolute path</span>
<span class="nv">CREDS_FILE</span><span class="o">=</span>/home/fetch/wifi-adapter-settings/hotspot_creds.cfg
<span class="k">if</span> <span class="o">[</span> <span class="nt">-f</span> <span class="nv">$CREDS_FILE</span> <span class="o">]</span><span class="p">;</span> <span class="k">then
    </span><span class="nb">source</span> <span class="nv">$CREDS_FILE</span>
    <span class="k">if </span>nmcli dev wifi | <span class="nb">grep</span> <span class="nt">-q</span> <span class="s2">"</span><span class="nv">$SSID</span><span class="s2">"</span><span class="p">;</span> <span class="k">then
        </span><span class="nv">ip_address</span><span class="o">=</span><span class="si">$(</span>ip addr show alpha_wifi_adap | <span class="nb">grep</span> <span class="s1">'inet '</span> | <span class="nb">awk</span> <span class="s1">'{print $2}'</span> | <span class="nb">cut</span> <span class="nt">-d</span>/ <span class="nt">-f1</span><span class="si">)</span>
        <span class="nb">echo</span> <span class="s2">"Hotspot '</span><span class="nv">$SSID</span><span class="s2">' is LIVE."</span>
        <span class="nb">echo</span> <span class="nt">-e</span> <span class="s2">"- IP: </span><span class="nv">$ip_address</span><span class="s2">
- Password: </span><span class="nv">$PASSWORD</span><span class="s2">"</span>
    <span class="k">else
        </span><span class="nb">echo</span> <span class="s2">"Hotspot '</span><span class="nv">$SSID</span><span class="s2">' is NOT ACTIVE."</span>
    <span class="k">fi
else
    </span><span class="nb">echo</span> <span class="s2">"Hotspot credentials file not found."</span>
<span class="k">fi</span>
</code></pre></div></div> <center> <div style="display: flex; align-items: center;"> <img src="/assets/blog/irvl-fetch-wifi-adapter-diy/alpha_adapter/step-5-out.png" alt="Step 5 Output" style="width: 45%; margin-right: 20px;"/> <video class="demo-video" width="58%" height="auto" controls=""> <source src="../../../assets/blog/irvl-fetch-wifi-adapter-diy/alpha_adapter/connect-hololens2-to-robot-hotspot.mp4" type="video/mp4"/> Your browser does not support the video tag. </video> </div> </center> <center><b>Left:</b> Hotspot status after system boot.<br/><b>Right:</b> A demo showcasing the HoloLens connecting to the created hotspot.</center> <p><br/></p> <hr/> <h3 id="troubleshooting">Troubleshooting</h3> <ul> <li><strong>Error: Not authorized to control network:</strong> <ul> <li>Ensure the script is executed with root privileges.</li> </ul> </li> <li><strong>Error: either ‘dev’ is duplicate or ‘iwlist’ is garbage:</strong> <ul> <li>Verify the hotspot interface name (alpha_wifi_adap).</li> <li>Double-check the hotspot script syntax.</li> </ul> </li> <li><strong>Hotspot not starting on boot:</strong> <ul> <li>Check the service logs: <div class="language-shell highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="nb">sudo </span>journalctl <span class="nt">-u</span> start_hotspot.service
</code></pre></div> </div> </li> </ul> </li> </ul> <p>🛠️ Be ready to debug as needed! 🧩</p> <hr/> <p>Feel free to reach out in case you have a query. You are always welcome. You can find me on X at <a href="https://x.com/jishnu_jaykumar" target="_blank">@jishnu_jaykumar</a>.</p> <script>document.querySelectorAll(".demo-video").forEach(function(e){e.playbackRate=1.5});</script>]]></content><author><name></name></author><category term="research"/><category term="robotics"/><category term="phd-life"/><category term="sys_setup"/><category term="robot"/><category term="wifi"/><category term="hotspot"/><category term="network"/><category term="system"/><category term="setup"/><summary type="html"><![CDATA[Robots operating in environments without reliable network connectivity often require a self-hosted Wi-Fi hotspot for seamless operation. This blog post will guide you through creating a Wi-Fi hotspot on boot using a Linux-based system, ensuring uninterrupted communication with your robot.]]></summary></entry><entry><title type="html">Using Docker for Academic Research</title><link href="https://jishnujayakumar.github.io/blog/2022/using-docker-for-academic-research/" rel="alternate" type="text/html" title="Using Docker for Academic Research"/><published>2022-07-06T23:54:24+00:00</published><updated>2022-07-06T23:54:24+00:00</updated><id>https://jishnujayakumar.github.io/blog/2022/using-docker-for-academic-research</id><content type="html" xml:base="https://jishnujayakumar.github.io/blog/2022/using-docker-for-academic-research/"><![CDATA[<p><img src="../../../assets/blog/docker-for-research.svg" alt="Docker for academic research" width="100%" height="150px"/></p> <center>Image made using logos available <a href="https://www.docker.com/company/newsroom/media-resources/" target="_blank">here</a>.</center> <p><br/></p> <hr/> <blockquote> <p><strong>Note</strong>: Reading this post needs familiarity with <a href="https://www.docker.com/" target="_blank">Docker</a>. If you are new to the Docker world, I suggest looking <a href="https://www.youtube.com/watch?v=iqqDU2crIEQ" target="_blank">here</a>. This post is more catered towards the steps required for creating and pushing images to <a href="https://hub.docker.com/">DockerHub</a>.</p> </blockquote> <p>Docker is a well-known tool. Although it is used prominently in the industry, there is a usage gap when talking about academic research. The main advantage of using Docker is that it allows the creation of images that ease the process of reproducibility. There have been various attempts to encourage reproducibility.</p> <ul> <li><a href="https://naacl2022-reproducibility-track.github.io/tutorial/" target="_blank">NAACL’22 Reproducibility Track</a></li> <li><a href="https://rescience.github.io/" target="_blank">RESCIENCE C</a></li> </ul> <p>It is always a good idea to do experiments inside the Docker container so that the image of the container can be shared at the end. This would give the end users the same environment for conducting experiments and reduce a lot of environment setup time.</p> <blockquote> <p><strong>Tip:</strong> The commit-and-push workflow below is the fastest way to snapshot a working research environment. For long-lived projects, also keep a <code class="language-plaintext highlighter-rouge">Dockerfile</code> alongside — it documents <em>how</em> the environment was built, which the research-software community recommends for reproducibility (see <a href="https://journals.plos.org/ploscompbiol/article?id=10.1371/journal.pcbi.1008316" target="_blank"><em>Ten simple rules for writing Dockerfiles for reproducible data science</em>, PLOS Computational Biology 2020</a>).</p> </blockquote> <p>Use the following steps to create and publish a Docker image for your project:</p> <p><strong>Step.1.a</strong>: Check for the image of interest locally.</p> <figure class="highlight"><pre><code class="language-bash" data-lang="bash">docker images</code></pre></figure> <p><strong>Step.1.b</strong>: If none, find an image of choice from <a href="https://hub.docker.com/">DockerHub</a> or any other source.</p> <p><strong>Step.2</strong>: Run the Docker image as a container.</p> <figure class="highlight"><pre><code class="language-bash" data-lang="bash">docker run <span class="nt">--gpus</span> all <span class="nt">-id</span> <span class="nt">--rm</span> <span class="se">\</span>
<span class="nt">-v</span> &lt;cloned-repo&gt;:/workspace <span class="se">\</span>
<span class="nt">--name</span> &lt;container-name&gt; &lt;docker-image&gt;</code></pre></figure> <p><strong>Step.3</strong>: Get inside the container.</p> <figure class="highlight"><pre><code class="language-bash" data-lang="bash">docker <span class="nb">exec</span> <span class="nt">-it</span> &lt;container-name&gt; /bin/bash <span class="c"># for bash shell</span></code></pre></figure> <p><strong>Step.4</strong>: Do the necessary changes, i.e., copy files, install packages, etc.</p> <p><strong>Step.5</strong>: Commit the changes.</p> <figure class="highlight"><pre><code class="language-bash" data-lang="bash">docker commit <span class="se">\</span>
<span class="nt">--author</span> &lt;author-email&gt; <span class="se">\</span>
<span class="nt">--message</span> &lt;commit-message&gt; <span class="se">\</span>
&lt;container-name&gt;</code></pre></figure> <p><strong>Step.6</strong>: Check for the IMAGE-ID of the newly committed image.</p> <figure class="highlight"><pre><code class="language-bash" data-lang="bash">docker images <span class="c"># get IMAGE-ID</span></code></pre></figure> <p><strong>Step.7</strong>: Tag the IMAGE-ID with the local-name.</p> <figure class="highlight"><pre><code class="language-bash" data-lang="bash">docker tag &lt;IMAGE-ID&gt; &lt;local-name&gt;</code></pre></figure> <p><strong>Step.8</strong>: Login using <a href="https://hub.docker.com/">DockerHub</a> username and password.</p> <figure class="highlight"><pre><code class="language-bash" data-lang="bash">docker login <span class="nt">-u</span> &lt;username&gt;
<span class="c"># enter password on prompt</span></code></pre></figure> <p><strong>Step.9</strong>: Create a repository on <a href="https://hub.docker.com/">DockerHub</a> before proceeding, i.e. <code class="language-plaintext highlighter-rouge">&lt;username&gt;/&lt;repo&gt;</code>.</p> <p><strong>Step.10</strong>: Tag IMAGE-ID with <a href="https://hub.docker.com/">DockerHub</a> <code class="language-plaintext highlighter-rouge">&lt;username&gt;/&lt;repo&gt;</code>.</p> <figure class="highlight"><pre><code class="language-bash" data-lang="bash">docker tag &lt;IMAGE-ID&gt; &lt;username&gt;/&lt;repo&gt;:&lt;tag&gt;</code></pre></figure> <p><strong>Step.11</strong>: Push the image to <a href="https://hub.docker.com/">DockerHub</a>.</p> <figure class="highlight"><pre><code class="language-bash" data-lang="bash">docker push &lt;username&gt;/&lt;repo&gt;</code></pre></figure> <p><br/><br/> Feel free to reach out in case you have a query. You are always welcome. <br/> Please post it as a tweet to <a href="https://x.com/jishnu_jaykumar" target="_blank">@jishnu_jaykumar</a>, and I will definitely try to answer it.</p> <figure class="highlight"><pre><code class="language-python" data-lang="python"><span class="nd">@jishnu_jaykumar</span> <span class="c1">#askjishnu &lt;your question&gt;</span></code></pre></figure>]]></content><author><name></name></author><category term="research"/><category term="Docker"/><category term="research"/><summary type="html"><![CDATA[Need for Docker in academic research]]></summary></entry></feed>